fun main() {
    val numeros = 10 // alterar par ao numeros conforme necessário
    var soma = 0

    // usando o loop para calclar a soma dos primeiros N númeos naturais

    for (i in 1..numeros) {
       soma += i
       
    }

    println("A soma dos primeiros $numeros numeros é : $soma")
}
